# Web フレームワーク
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# チャート計算（Swiss Ephemeris ベース）
kerykeion>=5.12,<6

# DB 接続・ORM・マイグレーション（Phase 1 で本格使用）
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.30
alembic>=1.13.1

# 設定管理（.env 読み込み）
pydantic-settings>=2.2.1

# 外部サービス接続（出生地検索）
httpx>=0.27.0
