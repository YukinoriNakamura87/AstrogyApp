# 占星術リーディング補助アプリ
 
## WHY（なぜ作るか）
 
占星術師（ゆき氏）が鑑定業務のボトルネックを解消するための個人専用ツール。
「チャートを見ながら解釈メモを取る」作業が最も時間がかかるため、チャート・メモ・クライアント管理を一本化する。
まず自分自身が使い効果を実感した上で、将来的なサービス化を検討する「ドッグフーディング型」開発。
 
## WHAT（何を作るか）
 
- クライアント管理（基本情報・チャート・セッション履歴）
- ネイタルチャート計算・表示（Kerykeion使用）
- チャート解釈メモ（天体ごとに紐づけ・上書き可能）
- セッション記録（セッションごとに独立して蓄積）
- 解釈辞書（天体×サイン/ハウス/アスペクト単位のナレッジベース）
詳細は `docs/` 以下を参照：
- `docs/企画書.md` — 機能スコープ・コアバリュー
- `docs/アーキテクチャ設計.md` — 技術スタック・データモデル・API設計
- `docs/実装ロードマップ.md` — フェーズ別実装順序・完了条件
## HOW（技術スタック・構成）
 
| レイヤー | 技術 |
|----------|------|
| 開発環境 | Docker Compose（frontend / backend / db の3コンテナ） |
| フロントエンド | Nuxt.js（Vue.js）|
| バックエンド | Python / FastAPI |
| チャート計算 | Kerykeion（Swiss Ephemerisベース） |
| データベース | PostgreSQL |
 
```
astrology-app/
├── CLAUDE.md
├── docker-compose.yml
├── docs/
│   ├── 企画書.md
│   ├── アーキテクチャ設計.md
│   └── 実装ロードマップ.md
├── frontend/          # Nuxt.js
└── backend/           # FastAPI + Kerykeion
```
 
## コマンド
 
```bash
# 開発環境の起動（3コンテナ一括）
docker compose up
 
# バックエンドのみ起動
docker compose up backend
 
# コンテナ再ビルド（依存関係変更時）
docker compose up --build
 
# ログ確認
docker compose logs -f backend
```
 
※ 詳細なコマンドはPhase 0完了後にここへ追記する
 
## 開発方針
 
- **実装主体はClaude**。ゆき氏はコードレビュー・設計判断を担当
- **コメント・PR説明は設計意図を厚めに記載**（Pythonの文法詳細よりも「なぜこの設計か」を重視）
- **機能単位でまとめて実装 → レビュー**のサイクルで進める
- 開発機はMac（Docker Desktop）。ボリュームマウントのパフォーマンスに留意
## 実装フェーズ（現在地）
 
- [x] Phase 0: Docker環境構築
- [ ] Phase 1: バックエンド基盤（DBスキーマ・Kerykeion連携・CRUD API）
- [ ] Phase 2: フロントエンド実装（登録フォーム＋セッション画面）
- [ ] Phase 3: 拡張（一覧・検索・トランジット・辞書連携）
- [ ] Phase 4: 認証・調整・将来のサービス化検討
**最初の縦串（MVPの中の最小スコープ）：**
> クライアント登録 → ネイタルチャート計算（Kerykeion） → チャート確認 → 解釈メモ作成・保存
 
## 重要な設計判断（変えないこと）
 
- **ChartInterpretationMemo と Session は別エンティティ**
  - チャート解釈メモ：天体ごとに1つ、セッションを重ねるたびに上書き更新
  - セッション記録：セッションごとに独立して蓄積（上書きしない）
- **チャート計算結果はDBにキャッシュ**（再計算不要の設計）
- **解釈辞書はクライアント非依存**のナレッジベースとして独立
- **UIテーマは深紺×ゴールド**（確認済みモックアップに準拠）
- **Phase 1ではAI機能を搭載しない**（ツールとしての堅牢性を優先）
 